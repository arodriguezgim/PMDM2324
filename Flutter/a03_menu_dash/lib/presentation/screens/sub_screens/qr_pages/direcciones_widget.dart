import 'package:flutter/material.dart';

class DireccionesWidget extends StatelessWidget {
  const DireccionesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Direcciones Widget'),
     );
  }
}